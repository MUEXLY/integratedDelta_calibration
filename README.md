# integratedDelta_calibration
Scripts designed to calibrate simulation data to observations using embedded discrepancy GP emulators

To install the required libraries (numpy,scipy,matplotlib,pandas):
pip install -r requirements.txt

